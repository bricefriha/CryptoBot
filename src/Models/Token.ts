export default class Token {
    id: string = '';
    symbol: string = '';
    name: string  = '';

    // constructor(params) {
    //     this.id = params.id;
    //     this.symbol = params.symbol;
    //     this.name = params.name;
    // }
}